services:

  cms:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www
      - ./docker/cms.conf:/etc/apache2/sites-enabled/000-default.conf

  preview:
    build: .
    ports:
      - "8081:80"
    volumes:
      - .:/var/www
      - ./docker/preview.conf:/etc/apache2/sites-enabled/000-default.conf

  watcher:
    build: ./docker/watcher
    ports:
      - "8001:8001"
    volumes:
      - .:/var/www
